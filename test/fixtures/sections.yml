# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  heading: Instalation
  article: wnote
  note_order: "<%= [
    ActiveRecord::FixtureSet.identify(:install_rbenv),
    ActiveRecord::FixtureSet.identify(:install_ruby),
    ActiveRecord::FixtureSet.identify(:install_rails)
    ].join(',') %>"

two:
  heading: Rails_generate_scaffold
  article: wnote
  note_order: "<%= ActiveRecord::FixtureSet.identify(:markdown) %>"

three:
  heading: Introduction
  article: rails_howto
  note_order: "<%= ActiveRecord::FixtureSet.identify(:getting_started) %>"

four:
  heading: Introduction
  article: lua
  note_order:

