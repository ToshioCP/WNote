<%= form_for(@note, url: @redirect_path) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :section %><br>
    <%= f.collection_select(:section_id, @note.section.article.sections, :id, :heading, options = {}, html_options = {class: "form-control"}) %>
  </div>
  <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title, options={class: "form-control", placeholder: "title"} %>
  </div>
  <div class="form-group">
    <%= f.label :text %><br>
    <%= f.text_area :text, options={class: "form-control",rows: "10", placeholder: "text"} %>
  </div>
  <div style="margin-top:5px;">
    <%= f.submit options={class: "btn btn-primary"} %>
  </div>
<% end %>
